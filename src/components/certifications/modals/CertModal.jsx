import { useEffect } from 'react';

const CertModal = () => {
  // This component is no longer needed with our new design
  // We're directly linking to certificates instead of showing a modal
  return null;
};

export default CertModal; 